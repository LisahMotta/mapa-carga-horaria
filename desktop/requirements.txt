# A interface usa a biblioteca padrão do Python (tkinter).
# openpyxl é usada para exportar o Mapa em Excel (.xlsx):
openpyxl>=3.1
# pyinstaller é usada para GERAR o executável (dist/):
pyinstaller>=6.0
